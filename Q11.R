## Using sub() and gsub()


text <- "apple, apple, and apple"
new_text_sub <- sub("apple", "orange", text)
new_text_gsub <- gsub("apple", "orange", text)
print(new_text_sub)
print(new_text_gsub)

